// This is a JavaScript comment

// Define variables
var firstName = "John";
var lastName = "Doe";

// Concatenate and display strings
var fullName = firstName + " " + lastName;
console.log("Full Name: " + fullName);

// Perform arithmetic operations
var num1 = 10;
var num2 = 5;
var sum = num1 + num2;
console.log("Sum: " + sum);

// Use conditional statements
if (sum > 15) {
    console.log("Sum is greater than 15.");
} else {
    console.log("Sum is not greater than 15.");
}

// Create a simple function
function greet(name) {
    console.log("Hello, " + name + "!");
}

// Call the function
greet("Alice");

// Define an array
var fruits = ["apple", "banana", "cherry"];
console.log("Fruits: " + fruits);

// Loop through the array
for (var i = 0; i < fruits.length; i++) {
    console.log("Fruit " + (i + 1) + ": " + fruits[i]);
}
//declaring Variables
var x = 10; // Declaring a variable 'x' with a value of 10
let message = "Hello, World!"; // Declaring a variable 'message' with a string value
const isTrue = true; // Declaring a constant variable 'isTrue' with a Boolean value
//declaring a string
let name = "Alice"; // A string variable
let greeting = `Hello, ${name}!`; // Using template literals for string interpolation
console.log(greeting); // Output: Hello, Alice!
